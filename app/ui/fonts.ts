import { Open_Sans, Lusitana } from 'next/font/google';
 
export const defaultFont = Open_Sans({ subsets: ['latin'] });

export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'], });
